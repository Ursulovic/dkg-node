[build]
builder = "dockerfile"
dockerfilePath = "apps/agent/Dockerfile"

[deploy]
startCommand = "/app/apps/agent/docker-entrypoint.sh"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
